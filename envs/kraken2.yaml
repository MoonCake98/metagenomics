name: kraken_env
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - kraken2
  - pigz  # For faster gzip compression (optional)
  - gzip  # Ensure gzip is available
  - tar   # Needed for extracting databases